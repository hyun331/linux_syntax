# 특정 도메인의 ip 주소 매핑 조회
# 해당 도메인을 가지는 ip주소를 찾겠다
nslookup 도메인명

# 나의 ip 정보
ifconfig    # 윈도우는 ipconfig

# port - 하나의 ip에서 서비스를 구분지을 수 있는 구분자
# ip - 특정 서버를 구분지을 수 있는 주소
# ping - 회선이 정상적으로 연결되어 있는지 확인. ip만 확인함
    # ping 사용법 : ping xxx.xxx.xxx.xxx
# nc -zv - 특정 포트가 열려있는지 - 넷켓이라고 읽음
    # 443 포트는 https
# netstat
# ssh - 원격 접속 명령어. 원격에 있는 호스트에 접속. 한 컴퓨터에 여러명의 사용자가 동시 접속 가능함
    # 추후에 사용할 예정


